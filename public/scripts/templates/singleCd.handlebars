<div id="{{this.id}}" class="items">
        <div class="item-container">
            <div>
                <ul class="item-properties">
                  <li><h3>{{this.name}}</h3></li>
                    <li>${{this.price}}</li>
                    <li>{{this.artist}}</li>
                </ul>
                <img src="images/{{this.id}}.jpg" width="150">
                <ul class="item-properties">

                    <li><span>Category:</span> {{this.category}}</li>
                    <li><span>Rating:</span> {{this.rating}}</li>
                    <li><span>Duration:</span> {{this.duration}}</li>
                    <li><span>Number of songs:</span> {{this.songCount}}</li>
                </ul>
            </div>
        </div>
    </div>
